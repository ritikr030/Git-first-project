//5. Write a C program to enter month number between(1-12) and print number of days in month

#include <stdio.h>
int main () {

   int a;
   printf("enter your number: ");
   scaf("%d",&a);
     
     if (a );
     
    return 0;
}